(ocamllex
 (modules lexer))

(menhir
 (modules menhir_parser)
 (flags --explain --dump)
 (infer true))

(executable
 (public_name parser)
 (name parser)
 (libraries str zarith fmt extraction))

(env
 (dev
  (flags
   (:standard -warn-error -A))
  (ocamlopt_flags
   (:standard -O3))))
