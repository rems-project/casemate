(ocamllex
 (modules lexer))

(menhir
 (modules menhir_parser)
 (flags --explain --dump)
 (infer true))

(executable
 (public_name parser)
 (name parser)
 (preprocess (staged_pps ppx_import ppx_deriving.show))
 (libraries str zarith fmt cmdliner extraction))
